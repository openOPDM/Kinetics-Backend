package org.kinetics.dao.extension;

/**
 * Values of this enum will be persisted in {@link ExtensionMetaData}. So, do
 * not change order or drop any values
 * 
 * @author akaverin
 * 
 */
public enum ExtensionProperty {

	REQUIRED, SHOW_IN_GRID

}
